name: probeB
on: workflow_dispatch
permissions:
  contents: read
  packages: write
jobs:
  j:
    runs-on: ubuntu-24.04
    steps:
      - run: declare -p GITHUB_TOKEN >/dev/null 2>&1 && echo "B_TOKEN=set" || echo "B_TOKEN=unset"
