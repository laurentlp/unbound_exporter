name: probe
on: workflow_dispatch
permissions:
  contents: read
  packages: write
jobs:
  j:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          env | grep -a "^GITHUB_TOKEN=" | od -c | head -n 3
          echo "shell: $0"
          type GITHUB_TOKEN 2>&1 | head -n 1
